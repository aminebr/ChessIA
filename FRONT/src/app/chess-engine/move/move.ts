import { Board } from '../board/board';
import { Piece } from '../pieces/piece';

export class Move {
    constructor(board : Board , pieceToMove : Piece , destinationCoordinate : number) {
        
    }
}